const initialState = []

export function shoutReducer(state = initialState, action) {
    switch (action.type) {
        case "FETCH_SHOUTS":
            return state

        default:
            return state

    }
}