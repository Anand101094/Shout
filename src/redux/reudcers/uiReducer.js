const initialState = []

export function uiReducer(state = initialState, action) {
    switch (action.type) {
        case "FETCH_SHOUTS":
            return state

        default:
            return state

    }
}